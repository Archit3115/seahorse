{
  "_comment": "When you open Claude Code inside this cloned repo and trust the folder, Claude Code sees this marketplace + enabled plugins and offers to install them. A project-enabled plugin from a github source does not load until you install it (Claude Code shows the command). If it doesn't prompt on your version, run the two commands in the README. This only affects sessions started in this directory.",
  "extraKnownMarketplaces": {
    "seahorse": {
      "source": {
        "source": "github",
        "repo": "Archit3115/seahorse"
      }
    }
  },
  "enabledPlugins": {
    "seahorse@seahorse": true,
    "caveman@seahorse": true,
    "ponytail@seahorse": true
  }
}
