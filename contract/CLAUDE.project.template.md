# {{PROJECT_NAME}} — Project Claude Code Contract

Generated by Seahorse bootstrap. This file **overrides** the global `~/.claude/CLAUDE.md` on any conflict.
Fill every `{{...}}` from a real inspection of the repo. Delete guidance comments once filled.

## Stack
- Languages: {{LANGUAGES}}
- Package manager: {{PKG_MANAGER}}
- Framework(s): {{FRAMEWORKS}}
- Test runner: {{TEST_RUNNER}}
- Deploy target: {{DEPLOY_TARGET}}

## Layout
```
{{TREE}}
```

## Run & test
- Dev: `{{DEV_CMD}}`
- Test: `{{TEST_CMD}}`
- Lint / type-check: `{{LINT_CMD}}`
- Build: `{{BUILD_CMD}}`

## Model routing (Seahorse advisor→executor)
Inherits the global §2 table. Project-specific overrides:
- Advisor: Fable 5 (→ Opus 4.8 1M fallback). {{ADVISOR_NOTES}}
- Heavy build / research: Opus 4.8 1M. Light build: Sonnet 5. GPT specialist: `/codex:*`.
- {{ROUTING_OVERRIDES}}

## Knowledge graph
- Location: `graphify-out/` (built with `graphify .`).
- Rebuild trigger: {{KG_REBUILD}} (commit hook / manual).
- Query before grepping: `graphify query "..."`.

## Outputs
- PDFs: LaTeX via `tectonic`, source in `{{TEX_DIR}}`.
- Diagrams: Mermaid.

## CI/CD
- Pipeline: `.github/workflows/{{CI_FILE}}` — {{CI_STAGES}}.

## Hard rules (project-specific — edit these)
1. {{HARD_RULE_1}}
2. {{HARD_RULE_2}}

## Continuity
- Current phase / status: {{STATUS}}
- Update this file at the end of every meaningful chunk of work.
